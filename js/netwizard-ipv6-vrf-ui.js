/* NetWizard IPv6 and VRF UI v1 */
(function initNetWizardIpv6VrfUi(root){
'use strict';
function arr(v){return Array.isArray(v)?v:[];}
function text(tag,value){const el=document.createElement(tag);el.textContent=String(value==null?'':value);return el;}
function renderInto(container,project){if(!container)return null;container.textContent='';const engine=root.NetWizardIpv6Vrf;if(!engine)return null;const report=engine.validateProject(project||{});container.appendChild(text('div','IPv6 y VRF')).lastChild.className='card-t';const meta=text('p',`VRFs: ${report.vrfs.length} · Redes IPv6: ${report.networks.length} · Bloqueantes: ${report.counts.blocking} · Avisos: ${report.counts.warnings}`);container.appendChild(meta);const table=document.createElement('table');table.style.cssText='width:100%;border-collapse:collapse;font-size:13px';table.innerHTML='<thead><tr><th align="left">Elemento</th><th align="left">Tipo</th><th align="left">VRF</th><th align="left">Prefijo / RD</th></tr></thead>';const body=document.createElement('tbody');for(const v of report.vrfs){const tr=document.createElement('tr');[v.name||v.id,'VRF','—',v.routeDistinguisher||'—'].forEach(x=>{const td=text('td',x);td.style.cssText='padding:5px;border-top:1px solid rgba(127,127,127,.25)';tr.appendChild(td);});body.appendChild(tr);}for(const n of report.networks){const tr=document.createElement('tr');[n.name||n.id,'IPv6',n.vrfRef||'global',n.prefix||'—'].forEach(x=>{const td=text('td',x);td.style.cssText='padding:5px;border-top:1px solid rgba(127,127,127,.25)';tr.appendChild(td);});body.appendChild(tr);}table.appendChild(body);container.appendChild(table);if(report.issues.length){const ul=document.createElement('ul');for(const i of report.issues){const li=text('li',`${i.code}: ${i.message}`);ul.appendChild(li);}container.appendChild(ul);}return report;}
function inject(){if(!root.document)return;const host=document.getElementById('pg-cfg')||document.getElementById('productionGateOut')?.parentElement;if(!host)return;let panel=document.getElementById('nwIpv6VrfPanel');if(!panel){panel=document.createElement('div');panel.id='nwIpv6VrfPanel';panel.className='card';panel.style.marginTop='12px';host.appendChild(panel);}const state=root.NetWizardState;renderInto(panel,state&&state.getSnapshot?state.getSnapshot():root.S||{});}
function install(){if(!root.document)return false;document.addEventListener('nw:project:changed',inject);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(inject,100));else setTimeout(inject,0);return true;}
const api={version:'netwizard-ipv6-vrf-ui-v1',renderInto,inject,install};root.NetWizardIpv6VrfUi=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root.document)install();
})(typeof window!=='undefined'?window:globalThis);
