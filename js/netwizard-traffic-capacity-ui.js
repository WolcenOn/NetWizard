/* NetWizard Traffic Capacity UI v1 */
(function initNetWizardTrafficCapacityUi(root){
'use strict';
function text(tag,value,cls){const el=document.createElement(tag);if(cls)el.className=cls;el.textContent=String(value==null?'':value);return el;}
function inject(){if(!root.document||!root.NetWizardTrafficCapacity)return;const host=document.getElementById('pg-cfg')||document.getElementById('productionGateOut')?.parentElement;if(!host)return;let panel=document.getElementById('nwTrafficCapacityPanel');if(!panel){panel=document.createElement('div');panel.id='nwTrafficCapacityPanel';panel.className='card';panel.style.marginTop='12px';host.appendChild(panel);}const project=root.NetWizardState&&root.NetWizardState.getSnapshot?root.NetWizardState.getSnapshot():{};const report=root.NetWizardTrafficCapacity.validateProject(project);panel.textContent='';panel.appendChild(text('div','Perfiles de tráfico y análisis de capacidad','card-t'));panel.appendChild(text('p',`Perfiles: ${report.profiles.length} · Bloqueantes: ${report.counts.blocking} · Avisos: ${report.counts.warnings}`,'hint'));if(!report.analyses.length){panel.appendChild(text('p','Sin perfiles o enlaces con capacidad para analizar.'));return report;}const table=document.createElement('table');table.style.cssText='width:100%;border-collapse:collapse;font-size:13px';const head=document.createElement('tr');['Ámbito','Capacidad','Media','Pico','Crítico','Utilización'].forEach(v=>head.appendChild(text('th',v)));const thead=document.createElement('thead');thead.appendChild(head);table.appendChild(thead);const body=document.createElement('tbody');for(const a of report.analyses){const tr=document.createElement('tr');const vals=[a.label,a.capacityMbps==null?'—':`${a.capacityMbps} Mbps`,`${a.averageMbps} Mbps`,`${a.peakMbps} Mbps`,`${a.criticalMbps} Mbps`,a.utilizationPercent==null?'—':`${a.utilizationPercent.toFixed(1)}%`];vals.forEach(v=>{const td=text('td',v);td.style.cssText='padding:5px;border-top:1px solid rgba(127,127,127,.25)';tr.appendChild(td);});body.appendChild(tr);}table.appendChild(body);panel.appendChild(table);return report;}
function install(){if(!root.document)return false;document.addEventListener('nw:project:changed',inject);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(inject,100));else setTimeout(inject,0);return true;}
const api={version:'netwizard-traffic-capacity-ui-v1',inject,install};root.NetWizardTrafficCapacityUi=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root.document)install();
})(typeof window!=='undefined'?window:globalThis);
