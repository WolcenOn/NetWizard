/* =========================================================
   NetWizard Capability Registry v0.1
   Registro extensible de plataformas, modelos, versiones, licencias,
   capacidades y parametros requeridos.
========================================================= */
(function initNetWizardCapabilityRegistry(root){
'use strict';
const platforms=new Map();
function arr(v){return Array.isArray(v)?v:[];}
function clean(v){return String(v==null?'':v).trim();}
function clone(v){return v==null?v:JSON.parse(JSON.stringify(v));}
function versionParts(v){return clean(v).split(/[^0-9]+/).filter(Boolean).map(Number);}
function compareVersions(a,b){const A=versionParts(a),B=versionParts(b),n=Math.max(A.length,B.length);for(let i=0;i<n;i++){const x=A[i]||0,y=B[i]||0;if(x!==y)return x>y?1:-1;}return 0;}
function normalizeCapability(id,value){const base={id,status:'unsupported',parameters:{},minVersion:'',models:[],licenses:[],notes:[]};if(value===true)return Object.assign(base,{status:'supported'});if(value===false||value==null)return base;return Object.assign(base,clone(value),{id,status:clean(value.status||'supported')});}
function registerPlatform(def){if(!def||!clean(def.id))throw new Error('platform id required');const caps={};for(const [id,value] of Object.entries(def.capabilities||{}))caps[id]=normalizeCapability(id,value);const normalized={id:clean(def.id),vendor:clean(def.vendor),label:clean(def.label||def.id),os:clean(def.os||def.id),aliases:arr(def.aliases).map(clean),models:arr(def.models).map(clean),versionField:clean(def.versionField||'osVersion'),licenseField:clean(def.licenseField||'license'),capabilities:caps};platforms.set(normalized.id,normalized);return clone(normalized);}
function resolvePlatform(device){const key=clean(device&&device.platform||device&&device.vendorOs||device&&device.os);if(platforms.has(key))return clone(platforms.get(key));for(const p of platforms.values())if(p.aliases.includes(key))return clone(p);return null;}
function getCapability(deviceOrPlatform,id){const p=typeof deviceOrPlatform==='string'?platforms.get(deviceOrPlatform):resolvePlatform(deviceOrPlatform);return p&&p.capabilities[id]?clone(p.capabilities[id]):normalizeCapability(id,false);}
function hasValue(obj,path){const parts=String(path).split('.');let cur=obj;for(const part of parts){if(cur==null||!Object.prototype.hasOwnProperty.call(cur,part))return false;cur=cur[part];}return cur!==''&&cur!=null;}
function validateCapability(device,platform,id,requested){const cap=platform.capabilities[id]||normalizeCapability(id,false),issues=[];if(!requested)return issues;const prefix=`${platform.label}: ${id}`;if(cap.status==='unsupported'||cap.status==='planned')issues.push({code:'NW-CAP-001',severity:'error',blocking:true,category:'compatibility',deviceId:device.id,capability:id,message:`${prefix} no está disponible (${cap.status}).`});else if(cap.status==='partial')issues.push({code:'NW-CAP-002',severity:'warning',blocking:false,category:'compatibility',deviceId:device.id,capability:id,message:`${prefix} tiene soporte parcial; requiere revisión.`});const version=clean(device[platform.versionField]||device.osVersion||device.version);if(cap.minVersion&&(!version||compareVersions(version,cap.minVersion)<0))issues.push({code:'NW-CAP-003',severity:'error',blocking:true,category:'compatibility',deviceId:device.id,capability:id,message:`${prefix} requiere versión ${cap.minVersion} o superior; actual: ${version||'no indicada'}.`});const model=clean(device.model);if(cap.models&&cap.models.length&&model&&!cap.models.includes(model))issues.push({code:'NW-CAP-004',severity:'warning',blocking:false,category:'compatibility',deviceId:device.id,capability:id,message:`${prefix} no está validada para el modelo ${model}.`});const license=clean(device[platform.licenseField]||device.license);if(cap.licenses&&cap.licenses.length&&(!license||!cap.licenses.includes(license)))issues.push({code:'NW-CAP-005',severity:'error',blocking:true,category:'compatibility',deviceId:device.id,capability:id,message:`${prefix} requiere licencia: ${cap.licenses.join(', ')}; actual: ${license||'no indicada'}.`});for(const [name,rule] of Object.entries(cap.parameters||{})){if(rule&&rule.required&&!hasValue(device,name)&&!(requested&&typeof requested==='object'&&hasValue(requested,name)))issues.push({code:'NW-CAP-006',severity:'error',blocking:true,category:'compatibility',deviceId:device.id,capability:id,parameter:name,message:`${prefix} requiere el parámetro ${name}.`});}
return issues;}
function requestedCapabilities(project,device){const explicit=device.capabilitiesRequested||device.features||{};const out=Object.assign({},explicit);if(project.routing&&project.routing.strategy==='ospf')out.ospf=project.routing;if(project.highAvailability){if(arr(project.highAvailability.firstHopGroups).length)out.firstHopRedundancy=project.highAvailability;if(arr(project.highAvailability.tracking).length)out.tracking=project.highAvailability;}if(project.accessSecurity){if(project.accessSecurity.dhcpSnooping)out.dhcpSnooping=project.accessSecurity;if(project.accessSecurity.arpInspection)out.arpInspection=project.accessSecurity;if(project.accessSecurity.portSecurity)out.portSecurity=project.accessSecurity;}if(arr(project.linkAggregations).length)out.lacp=project.linkAggregations;if(project.management){if(project.management.aaa)out.aaa=project.management;if(project.management.snmpv3)out.snmpv3=project.management;}return out;}
function validateDevice(project,device){const platform=resolvePlatform(device);if(!platform)return[{code:'NW-CAP-000',severity:'warning',blocking:false,category:'compatibility',deviceId:device&&device.id,message:`No existe registro de capacidades para ${clean(device&&device.vendorOs)||'plataforma desconocida'}.`}];const requested=requestedCapabilities(project||{},device||{}),issues=[];for(const [id,value] of Object.entries(requested))issues.push(...validateCapability(device,platform,id,value));return issues;}
function validateProject(project){const issues=[];for(const device of arr(project&&project.devices))issues.push(...validateDevice(project,device));return{version:'netwizard-capability-validation-v1',ok:!issues.some(i=>i.blocking),issues,counts:{blocking:issues.filter(i=>i.blocking).length,warnings:issues.filter(i=>i.severity==='warning').length}};}
const common={staticRouting:true,ospf:{status:'supported',parameters:{routerId:{required:false}}},lacp:true,ssh:true,syslog:true,ntp:true,aaa:{status:'supported',parameters:{'aaa.secretAlias':{required:false}}},snmpv3:{status:'supported',parameters:{'snmp.authSecretAlias':{required:false},'snmp.privSecretAlias':{required:false}}}};
registerPlatform({id:'cisco_ios',vendor:'Cisco',label:'Cisco IOS/IOS-XE',aliases:['ios','iosxe'],capabilities:Object.assign({},common,{hsrp:true,vrrp:true,tracking:true,dhcpRelay:true,dhcpSnooping:true,arpInspection:true,portSecurity:true,bgp:{status:'planned'},vrf:{status:'partial',minVersion:'15.0'},ipv6:{status:'partial'}})});
registerPlatform({id:'juniper_junos',vendor:'Juniper',label:'Juniper Junos',aliases:['junos'],capabilities:Object.assign({},common,{vrrp:true,hsrp:false,tracking:{status:'partial'},dhcpRelay:true,dhcpSnooping:{status:'partial'},arpInspection:{status:'partial'},portSecurity:{status:'partial'},bgp:{status:'planned'},vrf:{status:'partial'},ipv6:{status:'partial'}})});
registerPlatform({id:'huawei_vrp',vendor:'Huawei',label:'Huawei VRP',aliases:['vrp'],capabilities:Object.assign({},common,{vrrp:true,hsrp:false,tracking:{status:'partial'},dhcpRelay:true,dhcpSnooping:{status:'partial'},arpInspection:{status:'partial'},portSecurity:{status:'partial'},bgp:{status:'planned'},vrf:{status:'partial'},ipv6:{status:'partial'}})});
registerPlatform({id:'mikrotik_routeros',vendor:'MikroTik',label:'MikroTik RouterOS v7',aliases:['routeros'],capabilities:Object.assign({},common,{vrrp:true,hsrp:false,tracking:{status:'partial',minVersion:'7.0'},dhcpRelay:true,dhcpSnooping:{status:'partial'},arpInspection:false,portSecurity:{status:'partial'},bgp:{status:'planned'},vrf:{status:'partial',minVersion:'7.0'},ipv6:{status:'partial',minVersion:'7.0'}})});
registerPlatform({id:'fortinet',vendor:'Fortinet',label:'FortiGate FortiOS',aliases:['fortios'],capabilities:Object.assign({},common,{vrrp:true,hsrp:false,tracking:true,dhcpRelay:true,firewallPolicies:true,nat:true,ipsec:{status:'planned'},sdwan:{status:'partial',minVersion:'6.4'},utm:{status:'planned',licenses:['utm','enterprise']},bgp:{status:'planned'},vrf:{status:'partial'},ipv6:{status:'partial'}})});
registerPlatform({id:'aruba_aoss',vendor:'Aruba',label:'Aruba AOS-Switch',aliases:['aos-switch'],capabilities:Object.assign({},common,{ospf:{status:'partial'},vrrp:{status:'partial'},hsrp:false,tracking:false,dhcpRelay:true,dhcpSnooping:true,arpInspection:{status:'partial'},portSecurity:true,bgp:false,vrf:false,ipv6:{status:'partial'}})});
const api={version:'netwizard-capability-registry-v1',registerPlatform,resolvePlatform,getCapability,validateDevice,validateProject,compareVersions,listPlatforms:()=>Array.from(platforms.values()).map(clone)};root.NetWizardCapabilityRegistry=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;
})(typeof window!=='undefined'?window:globalThis);