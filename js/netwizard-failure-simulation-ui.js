/* NetWizard Failure Simulation UI v1 */
(function initNetWizardFailureSimulationUi(root){
'use strict';
function arr(v){return Array.isArray(v)?v:[];}
function text(tag,value){const el=document.createElement(tag);el.textContent=String(value==null?'':value);return el;}
function renderInto(container,project){if(!container)return null;container.textContent='';const engine=root.NetWizardFailureSimulation;if(!engine)return null;const report=engine.validateProject(project||{});container.appendChild(text('div','Simulación de fallos')).firstChild?.classList?.add('card-t');for(const s of report.scenarios){const row=document.createElement('div');row.style.cssText='padding:8px 0;border-top:1px solid rgba(127,127,127,.25)';row.appendChild(text('strong',s.name));row.appendChild(text('div',`Estado: ${s.status} · dispositivos afectados: ${s.failedDevices.length} · servicios no disponibles: ${s.unavailableServices.length} · WAN activos: ${s.activeCircuitIds.length}`));for(const issue of arr(s.issues))row.appendChild(text('div',issue.message));container.appendChild(row);}if(!report.scenarios.length)container.appendChild(text('p','No hay escenarios de fallo definidos.'));return report;}
function inject(){if(!root.document)return;const host=document.getElementById('pg-cfg')||document.getElementById('productionGateOut')?.parentElement;if(!host)return;let panel=document.getElementById('nwFailureSimulationPanel');if(!panel){panel=document.createElement('div');panel.id='nwFailureSimulationPanel';panel.className='card';panel.style.marginTop='12px';host.appendChild(panel);}const state=root.NetWizardState;renderInto(panel,state&&state.getSnapshot?state.getSnapshot():root.S||{});}
function install(){if(!root.document)return false;document.addEventListener('nw:project:changed',inject);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(inject,100));else setTimeout(inject,0);return true;}
const api={version:'netwizard-failure-simulation-ui-v1',renderInto,inject,install};root.NetWizardFailureSimulationUi=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root.document)install();
})(typeof window!=='undefined'?window:globalThis);
